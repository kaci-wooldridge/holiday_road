import { getItineraries } from "../data/DataAccess.js";

// on clicking the 'Get Directions' Button:
export const getDirections = (itineraryObj) => {};
// get/find/match the itinerary object for that trip
// use the geocaching API to convert all of locations to lat/long
// send all 4 lat/long pairs to the URL for the routing API
// display the directions from the routing api
